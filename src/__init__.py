"""
Core business logic for Face Clustering Analyzer.

Pages (Streamlit scripts) should import from `src.*` and store runtime state in
`st.session_state` (initialized via `src.state.ensure_state()`).
"""

