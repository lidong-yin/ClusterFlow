streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.18
pyarrow>=12.0
scikit-learn>=1.3
tqdm>=4.66
pillow>=10.0
pyarrow

# Optional (recommended):
# - faiss: used for fast TopK search / scatter analysis (GPU preferred, CPU fallback)
#   - pip: faiss-cpu (availability varies by platform), or conda install -c conda-forge faiss-cpu
# - infomap: used for Infomap clustering
#   - pip: infomap

